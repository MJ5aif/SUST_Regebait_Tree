{
  "version": "2.0.0",
  "tasks": [{
    "label": "cp",
    "type": "shell",
    "command": "g++ -Wall -std=c++20 -O2 -DLOCAL \"${file}\" -o \"${fileDirname}/${fileBasenameNoExtension}\" && \"${fileDirname}/${fileBasenameNoExtension}\" < input.in > output.out 2> error.log",
    "group": "build",
    "presentation": { "reveal": "silent" },
    "problemMatcher": ["$gcc"]
  }]
}